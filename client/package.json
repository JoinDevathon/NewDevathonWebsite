{
  "name": "client",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "export NODE_ENV=development; unset SERVER; rollup -c --watch",
    "server": "export NODE_ENV=production; export SERVER=true; rollup -c",
    "build": "export NODE_ENV=production; unset SERVER; rollup -c",
    "webpack-dev": "export NODE_ENV=development; unset SERVER; webpack --config webpack/config.dev.js --watch",
    "webpack-prod": "export NODE_ENV=production; unset SERVER; webpack --config webpack/config.prod.js",
    "webpack-server": "export NODE_ENV=production; unset SERVER; webpack --config webpack/config.server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "babel-loader": "^7.0.0",
    "babel-plugin-external-helpers": "^6.18.0",
    "babel-preset-es2015": "^6.18.0",
    "babel-preset-stage-3": "^6.17.0",
    "country-list": "^1.1.0",
    "svelte": "^1.22.4",
    "svelte-loader": "^2.0.1",
    "webpack": "^2.6.1"
  },
  "devDependencies": {
    "path": "^0.12.7"
  }
}
